import React from "react";
import PropTypes from "prop-types";
import { Container, Jumbotron } from "reactstrap";

const Login = props => {
	return <Container fluid>This is the Login page</Container>;
};

Login.propTypes = {};

export default Login;
